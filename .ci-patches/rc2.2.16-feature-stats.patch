diff --git a/src/utils/excelExport.ts b/src/utils/excelExport.ts
index 569d64e..65cfd8d 100644
--- a/src/utils/excelExport.ts
+++ b/src/utils/excelExport.ts
@@ -6 +6 @@ import { calculateStockSummary } from './inventoryUtils';
-import { formatDateDDMMYYYY } from './dateFormatter';
+import { formatDateDDMMYYYY, formatDateTime } from './dateFormatter';
@@ -7,0 +8 @@ import { saveWorkbookFile } from './fileExport';
+import { getCrewShiftCounts } from './crewUtils';
@@ -37,0 +39 @@ function autoFitColumns(ws: XLSX.WorkSheet) {
+
@@ -305,0 +308,3 @@ export function exportAllToExcel(params: {
+      'Ca Sáng (Người)': getCrewShiftCounts(item).morning,
+      'Ca Chiều (Người)': getCrewShiftCounts(item).afternoon,
+      'Ca Tối (Người)': getCrewShiftCounts(item).evening,
@@ -486,0 +492,3 @@ export function exportAllToExcelBase64(params: {
+      'Ca Sáng (Người)': getCrewShiftCounts(item).morning,
+      'Ca Chiều (Người)': getCrewShiftCounts(item).afternoon,
+      'Ca Tối (Người)': getCrewShiftCounts(item).evening,
@@ -522,0 +531,3 @@ export function exportCrewRecordsToExcel(crewRecords: CrewRecord[], teams: TeamI
+      'Ca Sáng (Người)': getCrewShiftCounts(item).morning,
+      'Ca Chiều (Người)': getCrewShiftCounts(item).afternoon,
+      'Ca Tối (Người)': getCrewShiftCounts(item).evening,
@@ -632 +643 @@ export function exportTeamStatisticsToExcel(params: {
-      { 'THÔNG TIN BÁO CÁO': 'NGÀY XUẤT', 'GIÁ TRỊ': new Date().toLocaleDateString('vi-VN') },
+      { 'THÔNG TIN BÁO CÁO': 'NGÀY XUẤT', 'GIÁ TRỊ': formatDateDDMMYYYY(new Date().toISOString().slice(0, 10)) },
@@ -743,0 +755,3 @@ export function exportTeamStatisticsToExcel(params: {
+      'Ca Sáng (Người)': getCrewShiftCounts(l).morning,
+      'Ca Chiều (Người)': getCrewShiftCounts(l).afternoon,
+      'Ca Tối (Người)': getCrewShiftCounts(l).evening,
diff --git a/src/utils/teamUtils.ts b/src/utils/teamUtils.ts
index d8a718b..a02eae7 100644
--- a/src/utils/teamUtils.ts
+++ b/src/utils/teamUtils.ts
@@ -3 +3 @@ import { normalizeUnit } from './unitUtils';
-import { getShiftDayFactor } from './crewUtils';
+import { getCrewShiftCounts } from './crewUtils';
@@ -416,6 +416,3 @@ export function calculateTeamStatistics(params: {
-    // Mandays & Crew statistics (Unified single worker count avoiding double-counting across multiple floor records for same shift)
-    const getWorkerCount = (r: CrewRecord) => r.workerCount || ((r.workersInside || 0) + (r.workersOutside || 0)) || 0;
-
-    // Group by date + shift to avoid double-counting the same team when one shift is
-    // recorded on multiple floors. Convert shifts to equivalent work-days so
-    // Sáng + Chiều = 1 công rather than 2 công.
+    // Mandays & Crew statistics. New records may have different headcount in
+    // morning/afternoon/evening. Group each shift independently so changing
+    // headcount across the day is reflected without double-counting floor rows.
@@ -424,7 +421,13 @@ export function calculateTeamStatistics(params: {
-      const shiftLabel = l.shift || 'default';
-      const shiftKey = `${l.date}_${shiftLabel}`;
-      const wc = getWorkerCount(l);
-      const existing = dateShiftMaxMap[shiftKey];
-      if (!existing || wc > existing.workerCount) {
-        dateShiftMaxMap[shiftKey] = { workerCount: wc, factor: getShiftDayFactor(shiftLabel), date: l.date };
-      }
+      const counts = getCrewShiftCounts(l);
+      ([
+        ['morning', counts.morning],
+        ['afternoon', counts.afternoon],
+        ['evening', counts.evening],
+      ] as const).forEach(([shiftKeyPart, workerCount]) => {
+        if (workerCount <= 0) return;
+        const shiftKey = `${l.date}_${shiftKeyPart}`;
+        const existing = dateShiftMaxMap[shiftKey];
+        if (!existing || workerCount > existing.workerCount) {
+          dateShiftMaxMap[shiftKey] = { workerCount, factor: 0.5, date: l.date };
+        }
+      });
