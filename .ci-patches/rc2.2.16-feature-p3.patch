diff --git a/src/components/ExportPdfModal.tsx b/src/components/ExportPdfModal.tsx
index 4defee7..59320d9 100644
--- a/src/components/ExportPdfModal.tsx
+++ b/src/components/ExportPdfModal.tsx
@@ -6,0 +7 @@ import { formatDateDDMMYYYY, formatDateTime, formatFloorName, parseLegacyTimesta
+import { getCrewShiftCounts } from '../utils/crewUtils';
@@ -24,0 +26,5 @@ const escapeHtml = (value: unknown): string => String(value ?? '')
+const getCrewShiftSummary = (record: CrewRecord): string => {
+  const { morning, afternoon, evening } = getCrewShiftCounts(record);
+  return [morning > 0 ? `Sáng ${morning}` : '', afternoon > 0 ? `Chiều ${afternoon}` : '', evening > 0 ? `Tối ${evening}` : ''].filter(Boolean).join(' · ') || '—';
+};
+
@@ -1005 +1011 @@ export const ExportPdfModal: React.FC<ExportPdfModalProps> = ({
-                  <td style="text-align: center;">${escapeHtml(c.shift || 'Hành chính')}</td>
+                  <td style="text-align: center;">${escapeHtml(getCrewShiftSummary(c))}</td>
@@ -1454 +1460 @@ Báo cáo từ Hệ Thống Quản Lý Thi Công & Nghiệm Thu
-              <label className={`p-2.5 rounded-xl border flex items-center gap-2 cursor-pointer transition-all ${includeChecklist ? 'bg-indigo-50/70 border-indigo-300 text-indigo-900 font-bold' : 'bg-slate-50 border-slate-200 text-slate-600'}`}>
+              {checklist.length > 0 && <label className={`p-2.5 rounded-xl border flex items-center gap-2 cursor-pointer transition-all ${includeChecklist ? 'bg-indigo-50/70 border-indigo-300 text-indigo-900 font-bold' : 'bg-slate-50 border-slate-200 text-slate-600'}`}>
@@ -1463 +1469 @@ Báo cáo từ Hệ Thống Quản Lý Thi Công & Nghiệm Thu
-              </label>
+              </label>}
diff --git a/src/components/GoogleConfigTab.tsx b/src/components/GoogleConfigTab.tsx
index e82f696..2f4a71e 100644
--- a/src/components/GoogleConfigTab.tsx
+++ b/src/components/GoogleConfigTab.tsx
@@ -39 +39 @@ import { getNumberFormatPreset, setNumberFormatPreset, NumberFormatPreset } from
-import { getDateFormatPreset, setDateFormatPreset, DateFormatPreset } from '../utils/dateFormatter';
+import { getDateFormatPreset, setDateFormatPreset, DateFormatPreset, formatDateTime } from '../utils/dateFormatter';
@@ -279 +279 @@ export const GoogleConfigTab: React.FC<GoogleConfigTabProps> = ({
-      const localTimeStr = localUpdatedAt ? new Date(localUpdatedAt).toLocaleString('vi-VN') : 'Chưa lưu lần nào';
+      const localTimeStr = localUpdatedAt ? formatDateTime(localUpdatedAt) : 'Chưa lưu lần nào';
@@ -284 +284 @@ export const GoogleConfigTab: React.FC<GoogleConfigTabProps> = ({
-        const cloudTimeStr = remoteUpdatedAt ? new Date(remoteUpdatedAt).toLocaleString('vi-VN') : 'Chưa rõ';
+        const cloudTimeStr = remoteUpdatedAt ? formatDateTime(remoteUpdatedAt) : 'Chưa rõ';
@@ -449 +449 @@ export const GoogleConfigTab: React.FC<GoogleConfigTabProps> = ({
-              <div><b>Sync cuối:</b> {syncDiagnostics.lastSyncAt > 0 ? new Date(syncDiagnostics.lastSyncAt).toLocaleString('vi-VN') : 'Chưa có'}</div>
+              <div><b>Sync cuối:</b> {syncDiagnostics.lastSyncAt > 0 ? formatDateTime(syncDiagnostics.lastSyncAt) : 'Chưa có'}</div>
@@ -782 +782 @@ export const GoogleConfigTab: React.FC<GoogleConfigTabProps> = ({
-                        {new Date(operation.deletedAt).toLocaleString('vi-VN')} · {operation.deletedItems?.length || 0} mục · còn {remainingDays} ngày
+                        {formatDateTime(operation.deletedAt)} · {operation.deletedItems?.length || 0} mục · còn {remainingDays} ngày
diff --git a/src/components/ProjectManagerModal.tsx b/src/components/ProjectManagerModal.tsx
index 67038c1..f2724a4 100644
--- a/src/components/ProjectManagerModal.tsx
+++ b/src/components/ProjectManagerModal.tsx
@@ -2216 +2216 @@ export const ProjectManagerModal: React.FC<ProjectManagerModalProps> = ({
-      const defaultName = cloudBackupName.trim() || `Sao lưu ${scopeLabel} (${new Date().toLocaleDateString('vi-VN')} ${new Date().toLocaleTimeString('vi-VN')})`;
+      const defaultName = cloudBackupName.trim() || `Sao lưu ${scopeLabel} (${formatDateTime(new Date())})`;
@@ -2843 +2843 @@ export const ProjectManagerModal: React.FC<ProjectManagerModalProps> = ({
-    return `${new Date(timestamp).toLocaleDateString('vi-VN')}${suffix}`;
+    return `${formatDateTime(timestamp).split(' ')[0]}${suffix}`;
@@ -2915 +2915 @@ export const ProjectManagerModal: React.FC<ProjectManagerModalProps> = ({
-        const updated = project.updatedAt ? new Date(Number(project.updatedAt)).toLocaleString('vi-VN') : 'Chưa rõ';
+        const updated = project.updatedAt ? formatDateTime(Number(project.updatedAt)) : 'Chưa rõ';
@@ -3708 +3708 @@ export const ProjectManagerModal: React.FC<ProjectManagerModalProps> = ({
-                              {new Date(b.createdAt).toLocaleString('vi-VN')}
+                              {formatDateTime(b.createdAt)}
diff --git a/src/components/SecurityModal.tsx b/src/components/SecurityModal.tsx
index f27ad66..701f3fe 100644
--- a/src/components/SecurityModal.tsx
+++ b/src/components/SecurityModal.tsx
@@ -42,0 +43 @@ import { confirmAsync } from '../utils/confirmAsync';
+import { formatDateTime } from '../utils/dateFormatter';
@@ -1361 +1362 @@ export const SecurityModal: React.FC<SecurityModalProps> = ({
-                            {new Date(log.timestamp).toLocaleString('vi-VN')}
+                            {formatDateTime(log.timestamp)}
diff --git a/src/lib/firebase.ts b/src/lib/firebase.ts
index 768254b..4255902 100644
--- a/src/lib/firebase.ts
+++ b/src/lib/firebase.ts
@@ -40,0 +41 @@ import { REALTIME_COLLECTIONS } from '../config/realtimeCollections';
+import { formatDateTime } from '../utils/dateFormatter';
@@ -2648 +2649 @@ export async function saveCloudBackup(backupName: string, allProjects: any[]): P
-      backupName: backupName || `Bản sao lưu ${new Date().toLocaleString('vi-VN')}`,
+      backupName: backupName || `Bản sao lưu ${formatDateTime(new Date())}`,
