ng(value || '').trim();
+  if (!raw) return '';
+  if (raw.startsWith('firestore:') || raw.startsWith('drive:')) {
+    return raw.split(':')[1] || '';
+  }
+  if (raw.startsWith('r2:') || raw.startsWith('storage:')) {
+    const objectPath = raw.slice(raw.indexOf(':') + 1).replace(/^\/+/, '');
+    const match = objectPath.match(/^projects\/([^/]+)\//);
+    return match?.[1] || '';
+  }
+  return '';
+}
+
+export async function getPhotoDataUrl(photoId: string, fallbackDataUrl?: string, useThumbnail = false, projectIdHint = ''): Promise<string> {
+  if (!photoId) return isDirectRenderablePhotoUrl(fallbackDataUrl) ? String(fallbackDataUrl) : '';
   try {
     if (useThumbnail) {
       const thumbVal = await localforage.getItem<Blob | string>(getPhotoThumbKey(photoId));
@@ -338,28 +362,34 @@ export async function getPhotoDataUrl(photoId: string, fallbackDataUrl?: string,
       }
     }
 
-    // Cloud photo binaries (primary Drive or legacy Firestore chunks) are downloaded
-    // lazily only when this image is actually displayed. The projectId is embedded
-    // in the opaque cloud reference: provider:projectId:fileId/photoId.
-    if (fallbackDataUrl && (fallbackDataUrl.startsWith('firestore:') || fallbackDataUrl.startsWith('drive:'))) {
+    // RC2.2.8 Runtime Golden: a second account/device usually has only Firestore
+    // photo metadata at first. Modern metadata points at private object storage with
+    // opaque `r2:` / `storage:` references; those strings are NOT browser image URLs.
+    // Resolve every cloud-backed photo through the authenticated provider adapter so
+    // VIEWER/EDITOR/ADMIN users in the same project can display the same binary.
+    const opaqueCloudRef = isOpaqueCloudPhotoReference(fallbackDataUrl);
+    const cloudProjectId = String(projectIdHint || projectIdFromCloudPhotoReference(fallbackDataUrl) || '').trim();
+    if (cloudProjectId && (opaqueCloudRef || !fallbackDataUrl)) {
       try {
-        const parts = fallbackDataUrl.split(':');
-        const projectId = parts[1] || '';
-        const cloudPhotoId = fallbackDataUrl.startsWith('firestore:') ? (parts[2] || photoId) : photoId;
+        const parts = String(fallbackDataUrl || '').split(':');
+        const cloudPhotoId = String(fallbackDataUrl || '').startsWith('firestore:') ? (parts[2] || photoId) : photoId;
         const { downloadPhotoBlobFromCloud } = await import('../lib/photoCloudSync');
-        const cloudBlob = await downloadPhotoBlobFromCloud(project