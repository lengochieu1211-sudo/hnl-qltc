 'asc' ? comparison : -comparison;
   });
-  const photoImageUrls = sortedPhotos.map(p => photoDataUrls[p.id] || p.localUri || p.cloudUrl || '');
+  const photoImageUrls = sortedPhotos.map(p => photoDataUrls[p.id] || p.localUri || (isDirectPhotoUrl(p.cloudUrl) ? p.cloudUrl! : ''));
   const imageUrls = photoImageUrls.filter(Boolean);
 
   return (
@@ -379,7 +384,7 @@ export const PhotoAttachmentPicker: React.FC<PhotoAttachmentPickerProps> = ({
       ) : (
         <div className="grid grid-cols-3 sm:grid-cols-4 gap-2">
           {sortedPhotos.map((photo, index) => {
-            const url = photoDataUrls[photo.id] || photo.localUri || photo.cloudUrl;
+            const url = photoDataUrls[photo.id] || photo.localUri || (isDirectPhotoUrl(photo.cloudUrl) ? photo.cloudUrl : '');
             return (
               <div
                 key={photo.id}
diff --git a/src/features/chat/ChatTab.tsx b/src/features/chat/ChatTab.tsx
index 0e2d0a9..5fc138a 100644
--- a/src/features/chat/ChatTab.tsx
+++ b/src/features/chat/ChatTab.tsx
@@ -40,7 +40,7 @@ const ChatImage: React.FC<{ projectId: string; attachment: ChatAttachment }> = (
     let objectUrl = '';
     (async () => {
       if (!photoId) return;
-      const local = await getPhotoDataUrl(photoId, '', true).catch(() => '');
+      const local = await getPhotoDataUrl(photoId, '', true, projectId).catch(() => '');
       if (local) {
         if (!cancelled) setSrc(local);
         return;
diff --git a/src/utils/photoStorage.ts b/src/utils/photoStorage.ts
index a22a8ea..b068d89 100644
--- a/src/utils/photoStorage.ts
+++ b/src/utils/photoStorage.ts
@@ -314,8 +314,32 @@ export async function mergeCloudPhotoMetadata(projectId: string, cloudPhotos: Ph
   }
 }
 
-export async function getPhotoDataUrl(photoId: string, fallbackDataUrl?: string, useThumbnail = false): Promise<string> {
-  if (!photoId) return fallbackDataUrl || '';
+function isOpaqueCloudPhotoReference(value?: string | null): boolean {
+  const raw = String(value || '').trim().toLowerCase();
+  return raw.startsWith('firestore:') || raw.startsWith('drive:') || raw.startsWith('r2:') || raw.startsWith('storage:');
+}
+
+function isDirectRenderablePhotoUrl(value?: string | null): boolean {
+  const raw = String(value || '').trim();
+  return raw.startsWith('blob:') || raw.startsWith('data:image/') || raw.startsWith('http://') || raw.startsWith('https://');
+}
+
+function projectIdFromCloudPhotoReference(value?: string | null): string {
+  const raw = Stri