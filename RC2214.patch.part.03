+  // generated HTML in a SECOND WebView, so a blob: URL created by the application
+  // WebView is not readable there. Resolve every attachment to an embedded data:image
+  // URL before enabling export. This also makes downloaded HTML portable/offline.
   const [crewPhotosMap, setCrewPhotosMap] = useState<Record<string, string[]>>({});
   const [defectPhotosMap, setDefectPhotosMap] = useState<Record<string, { before: string[]; after: string[] }>>({});
+  const [isResolvingReportPhotos, setIsResolvingReportPhotos] = useState(false);
+  const [reportPhotoError, setReportPhotoError] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!isOpen || !activeProjectId) return;
+    if (!isOpen || !activeProjectId) {
+      setCrewPhotosMap({});
+      setDefectPhotosMap({});
+      setIsResolvingReportPhotos(false);
+      setReportPhotoError(null);
+      return;
+    }
     let isMounted = true;
 
     async function loadPhotos() {
+      if (isMounted) {
+        setIsResolvingReportPhotos(true);
+        setReportPhotoError(null);
+      }
       try {
-        const photos = await getProjectPhotos(activeProjectId);
+        // requireBinary=true is intentional: never claim a PDF/HTML report is complete
+        // while shared R2 metadata exists but the authenticated binary cannot be read.
+        const photos = await getProjectPhotosWithBinary(activeProjectId, true);
         const cMap: Record<string, string[]> = {};
         const dMap: Record<string, { before: string[]; after: string[] }> = {};
 
         for (const p of photos) {
           if (!p.id || p.deleted) continue;
-          const url = await getPhotoDataUrl(p.id, p.cloudUrl || p.cloudFileId || p.localUri, false, activeProjectId);
-          if (!url) continue;
+          const embeddedUrl = String(p.base64 || p.localUri || '');
+          if (!embeddedUrl.startsWith('data:image/')) {
+            throw new Error(`Ảnh ${p.id} chưa có binary tự chứa để xuất báo cáo.`);
+          }
 
           if (p.entityType === 'crewRecord' && p.entityId) {
             if (!cMap[p.entityId]) cMap[p.entityId] = [];
-            cMap[p.entityId].push(url);
+            cMap[p.entityId].push(embeddedUrl);
           } else if (p.entityType === 'defect' && p.entityId) {
             if (!dMap[p.entityId]) dMap[p.entityId] = { before: [], after: [] };
             if (p.category === 'defect_after') {
-              dMap[p.entityId].after.push(url);
+              dMap[p.entityId].after.push(embeddedUrl);
             } else {
-              dMap[p.entityId].before.push(url);
+              dMap[p.entityId].before.push(embeddedUrl);
             }
           }
         }
@@ -184,7 +203,15 @@ export const ExportPdfModal: React.FC<ExportPdfModalProps> = ({
           setDefectPhotosMap(dMap);
         }
       } catch (err) {
-        console.warn('Failed to load photos for export:', err);
+        const message = err instanceof Error ? err.message : String(err || 'Không tải được ảnh báo cáo.');
+        console.warn('Failed to prepare portable report photos:', err);
+        if (isMounted) {
+          setCrewPhotosMap({});
+          setDefectPhotosMap({});
+          setReportPhotoError(message);
+        }
+      } finally {
+        if (isMounted) setIsResolvingReportPhotos(false);
       }
     }
 
@@ -1051,8 +1078,23 @@ export const ExportPdfModal: React.FC<ExportPdfModalProps> = ({
     `;
   };
 
+  const ensureReportPhotosReady = () => {
+    const needsAttachmentPhotos = includeCrewPhotos || includeDefectPhotos;
+    if (!needsAttachmentPhotos) return true;
+    if (isResolvingReportPhotos) {
+      alert('Đang tải và nhúng ảnh R2 vào báo cáo. Vui lòng chờ hoàn tất rồi xuất lại.');
+      return false;
+    }
+    if (reportPhotoError) {
+      alert(`Không thể xuất báo cáo có ảnh vì chưa tải đủ binary ảnh từ Cloud/R2.\n\n${reportPhotoError}`);
+      return false;
+    }
+    return true;
+  };
+
   // Direct HTML report file download for easy offline opening/printing
   const handleDownloadHtmlReport = async () => {
+    if (!ensureReportPhotosReady()) return;
     if (includeFloorPlan && isResolvingFloorPlansForExport) {
       alert('Đang tải ảnh mặt bằng từ Cloud để xuất báo cáo. Vui lòng chờ hoàn tất.');
       return;
@@ -1071,6 +1113,7 @@ export const ExportPdfModal: React.FC<ExportPdfModalProps> = ({
 
   // High-fidelity HTML Print / Save as PDF with FULL Unicode Vietnamese support (Có dấu 100%)
   const handlePrintHTML = () => {
+    if (!ensureReportPhotosReady()) return;
     if (includeFloorPlan && isResolvingFloorPlansForExport) {
       alert('Đang tải ảnh mặt bằng từ Cloud để xuất PDF. Vui lòng chờ hoàn tất.');
       return;
@@ -1160,6 +1203,7 @@ Báo cáo từ Hệ Thống Quản Lý Thi Công & Nghiệm Thu
   };
 
   const handleCopyHtmlReport = () => {
+    if (!ensureReportPhotosReady()) return;
     try {
       const htmlContent = getReportHtml();
       navigator.clipboard.writeText(htmlContent).then(() => {
diff --git a/src/lib/r2Storage.ts b/src/lib/r2Storage.ts
index 94693bf..342e6c7 100644
--- a/src/lib/r2Storage.ts
+++ b/src/lib/r2Storage.ts
@@ -87,13 +87,37 @@ function matchesExpected(actualSize: number, actualSha256: string | undefined, e
   return actualSize > 0 && sizeMatches && checksumMatches;
 }
 
+async function verifyR2ObjectViaAuthenticatedGet(
+  url: string,
